#include <thread>
#include <chrono>

void ExecuteBypassSkript()
{

    MessageBox(NULL, "Ola SS (PROCURANDO OQ AKI)", "OLA SS (PEGA NA PIKA DO LA?)", MB_OK);

    std::this_thread::sleep_for(std::chrono::seconds(2));

    return;
}